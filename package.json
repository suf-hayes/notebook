{
  "name": "notebook",
  "private": true,
  "version": "1.0.0",
  "description": "Cloudflare Worker wrapper for the notebook Eleventy site",
  "scripts": {
    "build": "cd site && npm ci && npm run build",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.110.0"
  }
}
